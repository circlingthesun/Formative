/*
	DEFINES PROGRAM SETTINGS
*/

var INTERVAL = 20;    // how often, in milliseconds, we check to see if a
                      // redraw is needed
var CURSOR_INTERVAL = 100;

// The selection color and width. Right now we have a red selection with a
// small width
var mySelColor = 'rgba(255,50,50,1.0)';
var mySelWidth = 1;
var mySelBoxColor = 'darkred'; // New for selection boxes
var mySelBoxSize = 6;

var MAX_HISTORY = 10;

var TEXTBOX_COLOUR = 'rgba(0,71,178,0.7)';
var CHECKBOX_COLOUR = 'rgba(178,0,71,0.7)';
var ARROW_COLOUR = 'rgba(255,50,50,1.0)';

var DRAG_SELECT_COLOUR = 'rgba(71,178,0,0.5)';
var DRAG_SELECT_STROKE_COLOUR = 'rgba(71,178,0,1)';

var IMG_UPLOAD_MIN_X = 1262;
var IMG_UPLOAD_MIN_Y = 1772;